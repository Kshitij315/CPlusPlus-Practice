#include <cstdio>

int main(void)
{
    short age;
    
    printf("Age: ");
    scanf("%hd", &age);
    
    float rate;
    /*
    if(age == 16)
        rate = 60;
    else if(age == 18)
        rate = 80;
    else if(age == 21)
        rate = 120;
    else if(age == 50)
        rate = 250;
    else
        rate = 50;
    */

    return 0;
}
